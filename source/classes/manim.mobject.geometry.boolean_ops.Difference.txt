# manim.mobject.geometry.boolean_ops.Difference

## Signature
class Difference ( subject , clip , ** kwargs )

## Summary
subject ( VMobject ) – The 1st VMobject . 
 clip ( VMobject ) – The 2nd VMobject 
 kwargs ( Any )

## Description
Difference ¶ 
 Qualified name: manim.mobject.geometry.boolean\_ops.Difference 

 class Difference ( subject , clip , ** kwargs ) [source] ¶ 
 Bases: _BooleanOps 
 Subtracts one VMobject from another one. 

 Parameters : 

 subject ( VMobject ) – The 1st VMobject . 
 clip ( VMobject ) – The 2nd VMobject 
 kwargs ( Any ) 

 Example 

 Example Placeholder 
 Placeholder block for DifferenceExample . 

```
 class DifferenceExample ( Scene ): 
 def construct ( self ): 
 sq = Square ( color = RED , fill_opacity = 1 ) 
 sq . move_to ([ - 2 , 0 , 0 ]) 
 cr = Circle ( color = BLUE , fill_opacity = 1 ) 
 cr . move_to ([ - 1.3 , 0.7 , 0 ]) 
 un = Difference ( sq , cr , color = GREEN , fill_opacity = 1 ) 
 un . move_to ([ 1.5 , 0 , 0 ]) 
 self . add ( sq , cr , un ) 

```

```

class DifferenceExample(Scene):
 def construct(self):
 sq = Square(color=RED, fill_opacity=1)
 sq.move_to([-2, 0, 0])
 cr = Circle(color=BLUE, fill_opacity=1)
 cr.move_to([-1.3, 0.7, 0])
 un = Difference(sq, cr, color=GREEN, fill_opacity=1)
 un.move_to([1.5, 0, 0])
 self.add(sq, cr, un)

```

 Methods 

 Attributes 

 always 
 Call a method on a mobject every frame. 

 animate 
 Used to animate the application of any method of self . 

 animation_overrides 

 color 

 depth 
 The depth of the mobject. 

 fill_color 
 If there are multiple colors (for gradient) this returns the first one 

 height 
 The height of the mobject. 

 n_points_per_curve 

 sheen_factor 

 stroke_color 

 width 
 The width of the mobject. 

 target 

 original_id 

 _original__init__ ( subject , clip , ** kwargs ) ¶ 
 Initialize self. See help(type(self)) for accurate signature. 

 Parameters : 

 subject ( VMobject ) 
 clip ( VMobject ) 
 kwargs ( Any ) 

 Return type : 
 None

## Examples

### Example 1
```python
class DifferenceExample ( Scene ): 
 def construct ( self ): 
 sq = Square ( color = RED , fill_opacity = 1 ) 
 sq . move_to ([ - 2 , 0 , 0 ]) 
 cr = Circle ( color = BLUE , fill_opacity = 1 ) 
 cr . move_to ([ - 1.3 , 0.7 , 0 ]) 
 un = Difference ( sq , cr , color = GREEN , fill_opacity = 1 ) 
 un . move_to ([ 1.5 , 0 , 0 ]) 
 self . add ( sq , cr , un )
```

### Example 2
```python
class DifferenceExample(Scene):
 def construct(self):
 sq = Square(color=RED, fill_opacity=1)
 sq.move_to([-2, 0, 0])
 cr = Circle(color=BLUE, fill_opacity=1)
 cr.move_to([-1.3, 0.7, 0])
 un = Difference(sq, cr, color=GREEN, fill_opacity=1)
 un.move_to([1.5, 0, 0])
 self.add(sq, cr, un)
```

## Source
https://docs.manim.community/en/v0.21.0/reference/manim.mobject.geometry.boolean_ops.Difference.html
